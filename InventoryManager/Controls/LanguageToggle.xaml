<?xml version="1.0" encoding="utf-8" ?>
<!-- Controls/LanguageToggle.xaml - Reusable language toggle component -->
<!-- This can be added to any page to provide language switching -->

<ContentView x:Class="InventoryManager.Controls.LanguageToggle"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Frame BackgroundColor="LightGray"
           CornerRadius="20"
           Padding="5"
           HasShadow="True"
           HeightRequest="40"
           WidthRequest="100">

        <Grid>
            <!-- Toggle Button -->
            <Button x:Name="ToggleButton"
                    Text="🇬🇧 EN"
                    BackgroundColor="Transparent"
                    TextColor="DarkBlue"
                    FontSize="14"
                    FontAttributes="Bold"
                    Clicked="OnToggleClicked" />

            <!-- Alternative: Segmented Control Style -->
            <!--
            <Grid ColumnDefinitions="*,*">
                <Button x:Name="EnButton"
                        Grid.Column="0"
                        Text="EN"
                        BackgroundColor="DarkBlue"
                        TextColor="White"
                        CornerRadius="15,0,0,15"
                        Clicked="OnEnglishClicked" />
                
                <Button x:Name="RoButton"
                        Grid.Column="1"
                        Text="RO"
                        BackgroundColor="Transparent"
                        TextColor="DarkBlue"
                        CornerRadius="0,15,15,0"
                        Clicked="OnRomanianClicked" />
            </Grid>
            -->
        </Grid>
    </Frame>

</ContentView>